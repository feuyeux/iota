# Iota Skill

`iota-skill/` 用于存放技能定义。

当前包含：

- `pet-generator/`：当请求包含“生成宠物”时，要求调用 `iota-fun/` 的多语言函数，组合生成宠物实例描述。

当前状态：

- 已创建 skill 规范文件
- 尚未把 skill 自动发现和执行接入 `iota-engine` 运行时

因此现在它是技能设计资产，不是已自动生效的执行路径。
